**/*.quarto_ipynb
/.jupyter_cache/
_site

# Quarto project cache — do not commit
/.quarto/idx/
/.quarto/project-cache/
/.quarto/xref/
# _freeze/ is intentionally tracked so CI can render with --no-execute
